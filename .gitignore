# rust build output
/target
# client output
/web
# images directory
/img
# nice to have, .cookie.txt keeps sneaking into the repo
/ignore

# this is where the logs go, woulnd't want that in the repo
/logs

# db files
*.db
*.sqlite

# for some reason git likes to sneak it in from time to time
.env
.env.production
